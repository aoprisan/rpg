{
  "name": "gothic-depths",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A Diablo 1-like dark gothic isometric action RPG with fully procedural assets, running in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pixi-filters": "^6.1.5",
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
